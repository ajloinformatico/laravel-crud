<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Empleados extends Model
{

    //hay que indicar el nombre de la TABLA y los campos en fileable si no no funciona
    //save() --> guarda en la tabla!!!
    //protected $table = "empleados";

    //Atributos que se van a asignar en masa
    /*
    protected $fillable = ['Nombre', 'ApellidoPaterno',
                        'ApellidoMaterno', 'Correo',
                        'Foto',
                        ];
    */


}
